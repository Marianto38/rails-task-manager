<h1> hola </h1>
<li><%= @task.title %> <%= @task.details%> <%= @task.completed%></li>
<%= link_to "Back to list", tasks_path  %>
<%= link_to "Edit task", edit_path  %>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure delete #{@task.title}?"}
%>
